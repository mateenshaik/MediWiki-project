

DockerFile:

1.Create a Dockerfile: Create a Dockerfile for MediaWiki that specifies the base  image, installs any dependencies, and copies the MediaWiki files.
2.Build the Docker image: Use the following command to build the Docker image,
3.Push the Docker image to a Docker hub with tag.

kubernetes:

1.Create a MySQL deployment: MediaWiki requires a MySQL database, so you will needto create a MySQL deployment in your Kubernetes cluster.
2.Create a PersistentVolumeClaim (PVC) for MySQL as well as gor mediawiki.
3.Create a Secret for MySQL.
4.Create a ConfigMap for MediaWiki: You should create a ConfigMap containing any configuration settings you want to apply to MediaWiki.
5.Create a Deployment for MediaWiki.

ansible:

1.i write simple ansible file for deploy mediawiki to kubernetes.



Note:

i used minikube to test mediawiki working or not.
